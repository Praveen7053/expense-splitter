server:
  port: 8080

spring:
  application:
    name: expense-splitter

  datasource:
    url: jdbc:mysql://localhost:3306/expensesplitter
    username: root
    password: seaface
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  jackson:
    serialization:
      indent_output: true
